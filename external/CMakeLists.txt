set(GLFW3_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/glfw" PARENT_SCOPE)

set(GLEW_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/glew" PARENT_SCOPE)
set(GLEW_USE_STATIC_LIBS OFF PARENT_SCOPE)
set(GLEW_VERBOSE OFF PARENT_SCOPE)

set(GLM_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/glm" PARENT_SCOPE)

add_subdirectory(stb)

set(ENV{FREETYPE_DIR} "${CMAKE_CURRENT_SOURCE_DIR}/freetype")

add_subdirectory(imgui)

set(FastNoise2_DIR "${CMAKE_CURRENT_SOURCE_DIR}/fastNoise2/lib/cmake/FastNoise2" PARENT_SCOPE)

add_subdirectory(njson)

add_subdirectory(entt)

set(TINYGLTF_HEADER_ONLY ON CACHE INTERNAL "" FORCE)
set(TINYGLTF_INSTALL OFF CACHE INTERNAL "" FORCE)
add_subdirectory(tinygltf)
